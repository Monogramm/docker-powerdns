dist: trusty
sudo: required

services: docker

language: bash

branches:
  only:
    - master
    - develop

before_script:
  - env | sort
  - name="powerdns"
  - image="docker-powerdns"
  - dir="."

script:
  - travis_retry docker build -t "$image" "$dir"
  - docker images
  - docker run --name "$name" -d "$image" "$dir" -e MYSQL_AUTOCONF=false -e BACKEND=bind
  - docker ps | grep "$name"
  - docker logs "$name"

notifications:
  email: false

env: # Environments
